# deps
node_modules/

# build outputs
dist/
dist-ssr/
*.local

# env / secrets
.env
.env.*
!.env.example
!.env.*.example
# Generado por `npm run env:local` (claves del stack local, pero no hay por que
# versionarlas: se regeneran con una linea).
apps/web/.env.development
# Secretos de las edge functions servidas localmente. Los de produccion van con
# `supabase secrets set`, jamas en un archivo del repo.
supabase/functions/.env

# logs
logs/
*.log
npm-debug.log*

# editor / os
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json

# supabase local
supabase/.temp/
supabase/.branches/
supabase/.env

# vercel
.vercel/